import { TransformDescriptionPipe } from './transform-description.pipe';

describe('TransformDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
