import { ConvertToImagePipe } from './convert-to-image.pipe';

describe('ConvertToImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToImagePipe();
    expect(pipe).toBeTruthy();
  });
});
