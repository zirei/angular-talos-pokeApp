import { ConvertGenderPipe } from './convert-gender.pipe';

describe('ConvertGenderPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertGenderPipe();
    expect(pipe).toBeTruthy();
  });
});
